<h1>Task一覧</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.id, task %> :<%= task.status %> &gt; : <%= task.title %> &gt; <%= task.content %></li>
  <% end %>
</ul>
<%= link_to '新規メッセージの投稿', new_task_path %>

